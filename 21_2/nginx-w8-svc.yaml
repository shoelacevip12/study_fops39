apiVersion: v1
kind: Service
metadata:
  name: nginx-wait-svc
  labels:
    role: den-w8
    app: nginx-w8
    organization: netology-fops40
    creator: denskv
spec:
  type: ClusterIP
  selector:
    app: nginx-w8
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80

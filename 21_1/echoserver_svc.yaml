apiVersion: v1
kind: Service
metadata:
  name: hello-world-svc
  labels:
    role: den-hello-world
    organization: my-test-org
    creator: denskv
spec:
  selector:
    app: my-test-hello-world-pod
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080

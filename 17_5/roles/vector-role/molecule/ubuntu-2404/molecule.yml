---
dependency:
  name: 'galaxy'
  enabled: true
driver:
  name: 'docker'
lint: |
  set -e
  yamllint .
  ansible-lint
platforms:
  - name: 'instance-ubuntu'
    image: 'antmelekhin/docker-systemd:ubuntu-24.04'
    volumes:
      - '/sys/fs/cgroup:/sys/fs/cgroup:rw'
      - '/var/lib/containerd'
    cgroupns_mode: 'host'
    privileged: true
    pre_build_image: true
    groups:
      - 'debian_family'
provisioner:
  name: 'ansible'
verifier:
  name: 'ansible'
...
